# CTF drain config parsed directly as TestConfig (FLAT — no [test] wrapper).
# verbosity = 1 (Debug) REQUIRED so covenant CMRs include debug symbols and
# match the on-chain taproot addresses.
mnemonic = "hole art knife walnut language cool borrow board rival silk october boy"
bitcoins = 0
verbosity = "Debug"

[esplora]
url = "https://blockstream.info/liquid/api"
network = "Liquid"
